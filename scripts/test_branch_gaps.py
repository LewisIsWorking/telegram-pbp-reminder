"""Original test_branch_gaps.py was 1477 lines, hitting every remaining
coverage gap. It violated the 200-line rule by 7x.

The tests have been extracted into themed sibling files named
``test_branch_gaps_NN_<topic>.py``, where the leading number gives the
bin order and the topic names the production package targeted.

This stub remains so the original module name still resolves (in case
any tooling or external reference depends on it). Tracking the split
in ``docs/dev/REFACTOR_PROGRESS.md``.
"""
