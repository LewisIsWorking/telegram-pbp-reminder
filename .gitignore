__pycache__/
*.pyc

# Secrets — never commit. Bridge tokens live in discord_bridge/.env locally.
.env
*.env

# Voice-bridge runtime log
*.log

# Coverage artefacts (run_coverage.py / pytest-cov write these locally)
.coverage
.coverage.*
htmlcov/
